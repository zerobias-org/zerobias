---
# create-connector BRIEF / MANIFEST — generated by /create-connector.
# Transient orchestration state. Lives at:
#   .connector/<vendor>-[<suite>-]<product>.md   (gitignored; never committed)
# It is the single source of truth across the 3 parts AND the payload that
# handoff prompts point at, so a fresh sub-repo session has full context.
connector: <vendor>[-<suite>]-<product>
created: <YYYY-MM-DD>
status: in-progress        # in-progress | blocked | done
---

# Connector brief — <Vendor> <Product>

## Resolved identity
| Field | Value |
|-------|-------|
| vendor (code)  | `<v>` |
| suite (code)   | `<s>` \| none |
| product (code) | `<p>` |
| author email   | `<git config user.email → CLAUDE.md table → ask>` |
| category       | `<short label, e.g. "SSE / Secure Web Gateway">` |
| auth           | `<method, e.g. "OAuth 2.0; legacy API key">` |
| OpenAPI spec   | `<url, HEAD-verified 200>` \| none |
| siblings       | `<other products under vendor/suite + catalog presence>` |
| suite decision | suite \| no-suite — `<one-line rationale>` |

## Schema targeting (concrete deferred; interface-targeted)
We collect directly to a base interface; the dataloader materializes a
`Dynamic<Interface>` concrete subclass at ingest via a discriminator.
Reference implementation: `collectorbot/package/google/gcp/iam`.

| Field | Value |
|-------|-------|
| schema package | `@zerobias-org/schema-zerobias-zerobias-base` (+ `-ts`) |
| interface(s)   | `<e.g. Principal / Secret / Provider / CloudEnvironment>` |
| discriminator  | `<kind \| type \| …Type>` |

## Package names (publish→consume chain)
| Layer | Package | Local-registry published? |
|-------|---------|---------------------------|
| product   | `@zerobias-org/product-<v>-<s?>-<p>`     | ☐ |
| module    | `@zerobias-org/module-<v>-<s?>-<p>`      | ☐ |
| collector | `@zerobias-org/collectorbot-<v>-<s?>-<p>`| n/a |

## Part status
- [ ] **Part 1 — VSP** (in-session, Option-A sub-agents)
  - [ ] vendor scaffold + `:gate` (gate-stamp.json)
  - [ ] suite scaffold + `:gate`  (only if suite)
  - [ ] product scaffold + `:gate`
  - [ ] product published to local registry
- [ ] **Part 2 — module** (handoff → `module/` session)
  - [ ] `/create-module` run
  - [ ] module built (`zbb build` green)
  - [ ] module published to local registry
- [ ] **Part 3 — collectorbot** (handoff → `collectorbot/` session)
  - [ ] `/create-collector` run (base-interface schema)
  - [ ] `npm run generate` → `generated/BaseClient.ts`
  - [ ] `/review-collector` compliance: ____%

## Exact resolved commands (defeat the arg-order traps)
| Step | Command (note differing arg orders) |
|------|-------------------------------------|
| vendor    | `cd vendor && sh scripts/createNewProduct.sh package/<v>` |
| suite     | `cd suite && sh scripts/createNewSuite.sh <verify signature: path vs "<v> <s>">` |
| module    | `cd module && /create-module <v> <p> [<s>]`   ← vendor **service** [suite] |
| collector | `cd collectorbot && /create-collector <v> [<s>] <p>` ← vendor [suite] **product** |

## Test harness
- slot: `cc-test`
- registry URL: `<REGISTRY_URL from: zbb --slot cc-test env get REGISTRY_URL>`

## Findings log (breakages → fixes to fold back into the skill)
- `<date>` — `<what broke>` — `<fix applied / TODO>`
